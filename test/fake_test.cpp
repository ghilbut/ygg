#include <gmock/gmock.h>

#include "fake.h"
#include "mock.h"


namespace test {


TEST(FakeTest, test) {

    //ConnectionDelegateMock conn0_delegate; 
    //Connection::Ptr conn0(FakeConnection::New());
    //conn0->BindDelegate(&conn0_delegate);

    //ConnectionDelegateMock conn1_delegate;
    //Connection::Ptr conn1(FakeConnection::New());
    //conn1->BindDelegate(&conn1_delegate);
}


}  // namespace test
